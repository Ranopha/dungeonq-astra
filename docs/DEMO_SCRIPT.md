# A short evaluator path

1. Open the independent Astra demo. Read the BROWSER MODEL / NO MODEL CALL label. The default case is already analyzed and awaiting modeled review.
2. Try unapproved apply. Observe a rejection and no receipt. Inspect the exact target and proposal digest; use Simulate reviewer approval, then Apply, Verify, Tamper a copy and Replay. One effect remains.
3. Select Budget exhausted, then Evidence + edge failure. Both must leave effects blocked; the traffic overlay must never fall back to real access.
4. Edit or load a synthetic Scenario Pack. Invalid input must be rejected without replacing the last valid session. The JSON stays in the browser.
5. Open the recorded Astra section and Verify recorded artifacts. Digests and the local receipt signature are checked now; the original seven runtime checks remain recorded observations. The altered copy must fail.
6. For actual Astra calls and authenticated review, follow [ASTRA.md](ASTRA.md). Bring your own key, inspect the budget, and do not film credentials.

When recording, retain the mode labels. Do not label automated reviewer fixtures as a person, recorded API output as a fresh model call, or local synthetic containment as production defense.
