# DungeonQ Astra source rules

This is the standalone Apache-2.0 Astra synthetic distribution. Preserve LICENSE, NOTICE and provenance. Never redirect a remote or deployment to the earlier Amazon or WebMCP entries. Their frozen sources, URLs and submissions are not this project's deployment targets.

Read README.md, SECURITY.md and docs/ASTRA.md. Use artificial data and bounded scenarios only. No production systems, scans, arbitrary shell/SQL or approval tool. Model candidates cannot grant authority. Keep reviewer credentials outside the model and the low-privilege MCP adapter.

Use free mock/local tests by default. Paid API calls require explicit per-run spending authority; no retry loops or repeated fresh labs to evade a total cap. Never publish a .env file, private lab, account metadata or API key. Recorded evidence is not a live call or proof of human presence.

Validate proportionately, then npm run check for release. Public availability, browser validation, local model observations, deployment and contest scheduling are distinct gates. Keep release provenance and do not overwrite user labs or reset credentials for convenience.
